<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= info?.company_name || 'JKT Hub' %></title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/styles2.css" />
    <link rel="stylesheet" href="/css/styles3.css" />
    <link rel="icon" type="image/x-icon" href="/public/images/JKT logo bg.png" />
    <script src="https://cdn.ckeditor.com/4.22.1/standard/ckeditor.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" integrity="sha512-..." crossorigin="anonymous" referrerpolicy="no-referrer" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body>
    <!-- Header -->
    <header class="header">
      <div class="logo-title">
        <% if (info?.logo_url) { %>
        <a href="/"><img src="<%= info.logo_url %>" alt="Logo" /></a>
        <% } %>
        <p><%= info?.company_name || 'C&S Church Online' %></p>
      </div>

      <button class="nav-toggle" onclick="toggleNav()">☰</button>
      <nav id="main-nav">
        <a class="active" href="/">Home</a>
        <!-- <a href="/pathways">Pathways</a> -->
        <a href="/courses">Courses</a>
        <a href="/about">About Us</a>
        <a href="/gallery">Gallery</a>
        <!-- <a href="#faq">FAQ</a> -->
        <% if (isLoggedIn) { %>
          <% if (users?.profile_picture) { %>
            <img src="<%= users.profile_picture %>" alt="Profile" style="width:32px;height:32px;border-radius:50%;vertical-align:middle;margin-right:8px;">
          <% } %>
          <a style="background-color: red;" href="/admin/logout" class="login-button">Logout</a>
          <div class="dropdown">
            <button class="dropbtn" onclick="toggleDropdown('profileMenu')">Profile ▾</button>
            <!-- <div class="dropdown-content" id="profileMenu">
              <a href="/profile">View Profile</a>
              <a style="background-color: black; padding: 5px 10px;" href="/make-payment" class="btn btn-primary">Fund wallet</a>
              <a href="/admin/dashboard">Dashboard</a>
              <a href="/admin/courses">Courses</a>
              <a href="/admin/pathways">Pathways</a>
              <a href="/admin/gallery">Gallery</a>
              <a href="/admin/videos">Videos</a>
            </div> -->

            <div class="dropdown-content" id="profileMenu">
              <p style="color: black;">💰 Wallet: ₦<%= walletBalance.toLocaleString() %></p>
              <a  href="/make-payment">Fund wallet</a>
              <a href="/profile">View Profile</a>
            </div>
          </div>
          
          
          
        <% } else { %>
          <a href="/admin/login" class="login-button">Login</a>
          <a
            href="/signup"
            style="background-color: rgb(0, 0, 0); color: rgb(255, 255, 255)"
            class="login-button"
          >Signup</a>
          
        <% } %>
      </nav>
    </header>

    <!-- Mission & Vision -->



    <% if (info?.hero_image_url) { %>
      <section class="hero-section" style="background-image: url('<%= info.hero_image_url %>');">
        <div class="hero-overlay">
          <h1>Welcome to <%= info?.company_name || 'Jaykirch Tech Hub' %></h1>
          <p><%= info?.vision || 'Empowering lives through the gospel of Christ.' %></p>
          <a href="/about" class="cta-btn">Know more about us</a>
          <!-- Optional trigger button -->
<!-- <button onclick="askNotificationPermission()" style="margin-top: 20px;">
  🔔 Enable Notifications
</button> -->
        </div>
        
      </section>
    <% } %>

    <!-- Marquee Message -->
    <% if (info?.marquee_message) { %>
      <div class="marquee-container">
        <div class="marquee-text"><%= info.marquee_message %></div>
      </div>
    <% } %>


   <!-- scripts for benefits display  -->
    <section id="benefits" class="section">
  <h2 style="text-align: center;">Benefits</h2>
  <div class="benefits-grid">
    <% benefits.forEach(benefit => { %>
      <div class="benefit-card">
        <div class="benefit-profile">
          <img src="<%= benefit.icon || '/images/default-user.png' %>" alt="Icon" />
        </div>
        <div class="benefit-details">
          <h4><%= benefit.title %></h4>
          <p><%= benefit.description %></p>
        </div>
      </div>
    <% }) %>
  </div>
</section>

<!-- <section class="event-section">
  <h2>📅 Upcoming Events</h2>
  <div class="event-grid">
    <% events.forEach(event => { %>
      <div class="event-card">
        <% if (event.image_url) { %>
          <img src="<%= event.image_url %>" alt="Event image" />
        <% } %>
        <h3><%= event.title %></h3>
        <p><%= event.event_date.toDateString() %> | <%= event.time %></p>
        <p><%= event.location %></p>
        <a href="/events/<%= event.id %>" class="btn-show">Show</a>
      </div>
    <% }) %>
  </div>
</section> -->

<section class="event_section">
  <h2>📅 Upcoming Events</h2>

  <% events.forEach(event => { %>
    <div class="event_container">
      <!-- Event Image -->
      <div class="event_details">
        <% if (event.image_url) { %>
          <img src="<%= event.image_url %>" alt="Event image" />
        <% } %>
      </div>

      <!-- Event Details -->
      <div class="event_details">
        <h2><%= event.title %></h2>
        <p><strong>Description:</strong> <%- event.description %></p>

        <p><strong>Date:</strong> <%= event.event_date.toDateString() %></p>
        <p><strong>Time:</strong> <%= event.time %></p>
        <p><strong>Location:</strong> <%= event.location %></p>

        <% if (event.is_paid) { %>
          <p><strong>Price:</strong> ₦<%= event.amount.toLocaleString() %></p>
        <% } else { %>
          <p><strong>This is a free event</strong></p>
        <% } %>
          <a href="/events/<%= event.id %>" class="btn-show">Show</a>
        <!-- <a href="/register/<%= event.id %>" class="btn-register">Register now</a> -->
      </div>
    </div>
  <% }) %>
</section>


   
    <!-- Pathways -->
    <section id="articles" class="section">
      <h2>Checkout our career pathways</h2>
      <div class="card-grid">
        <% career_pathways.forEach((pathway, idx) => { %>
          <div class="article-card">
            
            <% if (pathway.thumbnail_url) { %>
              <img
                src="<%= pathway.thumbnail_url %>"
                alt="Career_pathway Image"
                style="width: 100%; height: 300px; object-fit: cover; border-radius: 8px;"
              />
            <% } %>
            <h3><%- pathway.title %></h3> 
            <div class="article-body">
              <span id="article-short-<%= idx %>" style="display: block;">
                <%- pathway.description %>...
              </span>
              <!-- <span id="article-long-<%= idx %>" style="display: block;">
                
                <button class="continue-reading-btn" onclick="continueReading(<%= pathway.id %>)">Continue Reading</button>
              </span> -->
            </div>
            <!-- <small><%- new Date(pathway.created_at).toLocaleString() %></small> -->
          </div>
        <% }) %>
      </div>
      <div style="text-align: center; margin-top: 20px">
        <a href="/articles" class="login-button">View All pathway</a>
      </div>
    </section>

    <!-- course section -->
     <section id="courses" class="section">
        <h2 style="text-align: center;">Explore Our Courses</h2>
        <div class="horizontal-scroll-container" id="courseScroll">
          <% courses.forEach(course => { %>
            <div class="course-card scroll-card">
              <% if (course.thumbnail_url) { %>
                <img src="<%= course.thumbnail_url %>" alt="Course Thumbnail" />
              <% } %>
              <h4><%= course.title %></h4>
              <!-- <p><%- course.description.substring(0, 50) %>...</p> -->
              <small><strong><%= course.level %></strong> | <%= course.pathway_name || 'General' %></small>
            </div>
          <% }) %>
        </div>
         <div style="text-align: center; margin-top: 1rem;">
          <a href="/courses" class="login-button">View All Courses</a>
        </div>
      </section>

    
    <% if (isLoggedIn) { %>
      <!-- <a href="/make-payment" class="btn btn-primary">Make a Payment</a> -->
    <% } else { %>
      <!-- <a href=""> -->
        <!-- <button onclick="promptLogin()" class="btn btn-primary">Make a Payment</button> -->
      <!-- </a> -->
      <script>
        function promptLogin() {
          alert("Please login to make a payment.");
          window.location.href = "/admin/login";
        }
      </script>
    <% } %>
    

    <% if (subscribed === 'success') { %>

      <p style="color: green; text-align: center;">Thank you for subscribing!</p> <% } else if (subscribed === 'fail') { %> <p style="color: red; text-align: center;">There was a problem with your subscription.</p> <% } %>
    <footer class="site-footer">
      <div class="footer-container"> 
        <div class="footer-about"> 
            <a href="/"><img src="/images/JKT logo bg.png" alt="Logo" width="50px" height="50px"/></a>
            
          <h3>Jaykirch Tech Hub </h3> 
          <p>Welcome to the Future of Learning — Empowering Students, Teachers, and Schools with Smart Education Tools.</p>
         </div>

  <!-- 
          <div class="footer-about">
            <P>You can also support willingly</P>
            <h3>Account Name: Cherubim and Seraphim Church Online</h4>
            <h1 style="color: white;">Account Number: 0126607952</h1>
            <h3>Bank: wema bank</h3>
          </div> -->

      <div class="footer-social"> <h4>Connect with us</h4> 
        <div class="social-links"> 
          <a href="https://wa.link/wv7jtg" target="_blank" aria-label="WhatsApp"> 
            <i class="fab fa-whatsapp"></i> 
          </a> 

          <a href="https://www.facebook.com/share/14DpdxEmwVk/" target="_blank" aria-label="WhatsApp"> 
            <i class="fab fa-facebook"></i> 
          </a> 
          
          <a href="https://instagram.com/jkt_hub" target="_blank" aria-label="Instagram"> 
            <i class="fab fa-instagram"></i> 
          </a> 
          
          <a href="https://youtube.com/@JKT_hub" target="_blank" aria-label="YouTube"> 
            <i class="fab fa-youtube"></i> 
          </a> 
          
          <a href="https://t.me/Jaykirch" target="_blank" aria-label="Telegram"> 
            <i class="fab fa-telegram-plane"></i> 
          </a> 
          
          <a href="mailto:jaykirchtechhub@gmail.com" target="_blank" aria-label="Email"> 
            <i class="fas fa-envelope"></i> 
          </a>
        </div>
       </div>

      <div class="footer-subscribe">
        <h4>Subscribe to our newsletter</h4>
        <form method="POST" action="/subscribe" class="subscribe-form">
          <input
            type="email"
            name="email"
            placeholder="Enter your email"
            required
          />
          <button type="submit">Subscribe</button>
        </form>
      </div>
    </footer>

    <!-- script for faq toggle show/hide answer -->
    <!-- <script> 
      function toggleFaq(index) { 
        const answer = document.getElementById(`answer-${index}`); 
        const icon = document.getElementById(`icon-${index}`); 
        if (answer.classList.contains('hidden')) {
        answer.classList.remove('hidden'); 
          icon.classList.remove('fa-plus'); 
          icon.classList.add('fa-minus'); } 
        else { 
          answer.classList.add('hidden'); 
          icon.classList.remove('fa-minus'); 
          icon.classList.add('fa-plus'); 
        } 
      } 
    </script> -->

<!-- for redirect if user is not logged in -->
<script>
  
  const isLoggedIn = <%- JSON.stringify(isLoggedIn) %>;

  function handleProtectedLink(e, targetUrl) {
    if (!isLoggedIn) {
      e.preventDefault();
      // Redirect to login with redirect param
      window.location.href = '/admin/login?redirect=' + encodeURIComponent(targetUrl);
    }
    // else, allow default navigation
  }

  // Attach event listeners after DOM is loaded
  document.addEventListener('DOMContentLoaded', function() {
    const articlesBtn = document.querySelector('a[href="/articles"]');
    const videosBtn = document.querySelector('a[href="/videos"]');
    const faqBtn = document.querySelector('a[href="/faq"]');
    
    if (articlesBtn) {
      articlesBtn.addEventListener('click', function(e) {
        handleProtectedLink(e, '/articles');
      });
    }
    if (videosBtn) {
      videosBtn.addEventListener('click', function(e) {
        handleProtectedLink(e, '/videos');
      });
    }
    if (faqBtn) {
      faqBtn.addEventListener('click', function(e) {
        handleProtectedLink(e, '/faq');
      });
    }
  });
</script>

<!-- For the navigation toggle -->
<script>
  function toggleNav() {
    var nav = document.getElementById("main-nav");
    nav.classList.toggle("open");
  }
</script>

<!-- script for closing nav on outside click -->
<script>
  // Close nav when clicking outside of it
  document.addEventListener("click", function (event) {
    const nav = document.getElementById("main-nav");
    const toggleBtn = document.querySelector(".nav-toggle");

    if (
      nav.classList.contains("open") &&
      !nav.contains(event.target) &&
      !toggleBtn.contains(event.target)
    ) {
      nav.classList.remove("open");
    }
  });
</script>


<!-- script for carousel  -->
<!-- <script>
  let currentSlide = 0;
  const slides = document.querySelectorAll('.carousel-slide');

  function showSlide(idx) {
    slides.forEach((slide, i) => {
      slide.style.display = (i === idx) ? 'block' : 'none';
    });
    currentSlide = idx;
  }

  function nextSlide() {
    let next = (currentSlide + 1) % slides.length;
    showSlide(next);
  }

  function prevSlide() {
    let prev = (currentSlide - 1 + slides.length) % slides.length;
    showSlide(prev);
  }

  setInterval(nextSlide, 4000); // Change every 4 seconds
  if (slides.length > 0) showSlide(0);
</script> -->

<!-- script to preview announcement -->
<!-- <script>
  function showAnnouncement() {
    document.getElementById('announcement-preview').style.display = 'none';
    document.getElementById('announcement-section').style.display = 'block';
  }
  function hideAnnouncement() {
    document.getElementById('announcement-section').style.display = 'none';
    document.getElementById('announcement-preview').style.display = 'block';
  }
</script> -->

<!-- script for continue reading and check if user is logged in -->
<script>
  function showMore(idx) {
  document.getElementById('article-short-' + idx).style.display = 'none';
  document.getElementById('article-long-' + idx).style.display = 'block';
}
function showLess(idx) {
  document.getElementById('article-long-' + idx).style.display = 'none';
  document.getElementById('article-short-' + idx).style.display = 'block';
}
  function continueReading(articleId) {
    if (<%= isLoggedIn ? 'true' : 'false' %>) {
      window.location.href = '/articles/' + articleId;
    } else {
      window.location.href = '/admin/login?redirect=' + encodeURIComponent('/articles/' + articleId);
    }
  }
</script>

<!-- script for push notifications -->

<script>
  const vapidPublicKey = "<%= vapidPublicKey %>";

  if ('serviceWorker' in navigator && 'PushManager' in window) {
    window.addEventListener('load', async () => {
      const permission = Notification.permission;
      const lastPrompt = localStorage.getItem("lastPromptTime");
      const now = Date.now();

      // Only show banner if not granted and enough time has passed (24h)
      if (permission !== 'granted' && (!lastPrompt || now - Number(lastPrompt) > 86400000)) {
        document.getElementById('notif-banner').style.display = 'block';
      }
    });
  }

  async function triggerSubscription() {
    const permission = await Notification.requestPermission();
    localStorage.setItem("lastPromptTime", Date.now());

    if (permission !== 'granted') {
      alert("❌ Notification permission denied.");
      dismissBanner();
      return;
    }

    try {
      const registration = await navigator.serviceWorker.register('/sw.js');
      const subscription = await registration.pushManager.subscribe({
        userVisibleOnly: true,
        applicationServerKey: urlBase64ToUint8Array("<%= vapidPublicKey %>")
      });

      await fetch('/notifications/subscribe', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(subscription)
      });

      alert("✅ Subscribed to notifications!");
      localStorage.setItem("pushStatus", "granted");
      dismissBanner();
    } catch (err) {
      console.error("Subscription error:", err);
    }
  }

  function dismissBanner() {
    document.getElementById('notif-banner').style.display = 'none';
  }

  function urlBase64ToUint8Array(base64String) {
    const padding = "=".repeat((4 - base64String.length % 4) % 4);
    const base64 = (base64String + padding).replace(/-/g, "+").replace(/_/g, "/");
    const rawData = atob(base64);
    return Uint8Array.from([...rawData].map((char) => char.charCodeAt(0)));
  }
</script>

<!-- script for auto-scroll -->
     <!-- <script>
        let scrollInterval;
        let scrollContainer = document.getElementById("courseScroll");

        function startAutoScroll() {
          scrollInterval = setInterval(() => {
            scrollContainer.scrollLeft += 1;
            if (scrollContainer.scrollLeft + scrollContainer.clientWidth >= scrollContainer.scrollWidth) {
              scrollContainer.scrollLeft = 0;
            }
          }, 20); // Lower = faster
        }

        function stopAutoScroll() {
          clearInterval(scrollInterval);
        }

        scrollContainer.addEventListener("mouseenter", stopAutoScroll);
        scrollContainer.addEventListener("mouseleave", startAutoScroll);

        startAutoScroll(); // Start on load
      </script> -->


      <script>
  // const scrollContainer = document.querySelector(".course-scroll-container");
  let scrollContainer = document.getElementById("courseScroll");
  let scrollAmount = 1;
  let scrollInterval;

  function startAutoScroll() {
    scrollInterval = setInterval(() => {
      // Use Math.ceil for precision
      const maxScrollLeft = scrollContainer.scrollWidth - scrollContainer.clientWidth;
      if (Math.ceil(scrollContainer.scrollLeft) >= maxScrollLeft) {
        // Pause before looping
        clearInterval(scrollInterval);
        setTimeout(() => {
          scrollContainer.scrollLeft = 0;
          startAutoScroll();
        }, 1000); // 1-second pause before restarting
      } else {
        scrollContainer.scrollLeft += scrollAmount;
      }
    }, 20); // speed of scroll
  }

  scrollContainer.addEventListener("mouseenter", () => clearInterval(scrollInterval));
  scrollContainer.addEventListener("mouseleave", startAutoScroll);

  window.addEventListener("load", startAutoScroll);
</script>


<!-- Notification Banner -->
<!-- <div id="notif-banner" style="display: none; position: fixed; top: 0; left: 0; right: 0; background: #333; color: #fff; padding: 12px; text-align: center; z-index: 999;">
  <span>🔔 Get updates on new devotionals & articles!</span>
  <button onclick="triggerSubscription()" style="margin-left: 10px; padding: 6px 12px;">Enable</button>
  <button onclick="dismissBanner()" style="margin-left: 10px; padding: 6px 12px;">Dismiss</button>
</div> -->



</body>
</html>
